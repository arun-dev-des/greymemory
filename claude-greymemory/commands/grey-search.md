---
description: Search greymemory for relevant past memories
---

Search this project's greymemory for memories relevant to: "$ARGUMENTS"

Use the `grey_search` MCP tool (from the bundled `greymemory` server) with query "$ARGUMENTS",
then summarize the matches concisely. If nothing relevant is found, say so plainly.
