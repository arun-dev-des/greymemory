---
description: Save an explicit memory to greymemory
---

Save the following as a durable project memory: "$ARGUMENTS"

Use the `grey_add` MCP tool (from the bundled `greymemory` server) with text "$ARGUMENTS",
then confirm how many memories were stored.
